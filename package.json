{
  "name": "optional-peer-test",
  "version": "0.0.1",
  "description": "Testing npm behavior for installing optional peer dependencies",
  "scripts": {

  },
  "dependencies": {
    "@sap/hdi":"^4.5.2",
    "@sap/hdi-deploy": "^5.2.2"
  }
}
